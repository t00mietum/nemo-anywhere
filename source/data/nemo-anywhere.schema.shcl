# Schema for the nemo-anywhere config file.
# Declares every setting's type, default and (where closed) allowed values.
# Plain SHCL - nothing compiles it. See vendor/shcl for the language.

field: favorites
	type: string-array

field: favorites-root-metadata
	type: string-array

field: appearance.gtk-theme
	type: string
	default: 
	desc: Widget theme name, or empty for the platform's own

field: appearance.icon-theme
	type: string
	default: 
	desc: Icon theme name, or empty for the platform's own

field: appearance.mode
	type: string
	allowed: system, light, dark
	default: system

field: archive.create-with-7z
	type: string
	default: "{{PROGRAM}} a {{FORMAT}} {{LEVEL}} {{THREADS}} -y -bsp1 {{PASSWORD}} {{SPLIT}} {{SOLID}} {{LINKS}} -- {{TARGET_ARCHIVE}} {{SOURCE_ITEMS}}"
	desc: Command line 7-Zip is run with to create an archive. Each {{NAME}} stands for something the Compress dialog fills in; drop one and that option stops working. Empty the line for the built-in one.

field: archive.create-with-rar
	type: string
	default: "{{PROGRAM}} a {{LEVEL}} {{THREADS}} -r -y {{PASSWORD}} {{SPLIT}} {{SOLID}} {{DEDUPE}} {{RECOVERY}} {{LOCK}} {{LINKS}} -- {{TARGET_ARCHIVE}} {{SOURCE_ITEMS}}"
	desc: Command line rar is run with to create an archive. Same placeholders as the 7-Zip line, plus {{DEDUPE}}, {{RECOVERY}} and {{LOCK}}, which only rar has.

field: archive.extract-with-7z
	type: string
	default: "{{PROGRAM}} x -y -bsp1 {{PASSWORD}} -o{{TARGET_FOLDER}} -- {{SOURCE_ARCHIVE}}"
	desc: Command line 7-Zip is run with to unpack an archive. Empty the line for the built-in one.

field: archive.extract-with-rar
	type: string
	default: "{{PROGRAM}} x -y {{PASSWORD}} -- {{SOURCE_ARCHIVE}} {{TARGET_FOLDER_WITH_SEPARATOR}}"
	desc: Command line rar or unrar is run with to unpack an archive. Empty the line for the built-in one.

field: compact-view.all-columns-have-same-width
	type: bool
	default: false

field: compact-view.default-zoom-level
	type: string
	allowed: smallest, smaller, small, standard, large, larger, largest
	default: standard

field: interface.clock-use-24h
	type: bool
	default: true

field: media-handling.automount
	type: bool
	default: false

field: media-handling.automount-open
	type: bool
	default: false
	desc: Open a window on media as soon as it is mounted

field: performance.cpu-percent
	type: int
	default: 50
	desc: Share of the machine's CPU cores compression may use

field: privacy.remember-recent-files
	type: bool
	default: true

field: terminal.exec
	type: string
	default: ""
	desc: Terminal used for Open in Terminal, or empty to let the system pick

field: terminal.exec-arg
	type: string
	default: -e
	desc: Argument that terminal takes before a command

field: desktop.show-desktop-icons
	type: bool
	default: true

field: desktop.text-ellipsis-limit
	type: int
	default: 2
	desc: Lines of a name under a desktop icon before it is cut short

field: desktop.use-desktop-grid
	type: bool
	default: true

field: icon-view.captions
	type: string-array
	default: none, size, date_modified
	desc: Extra details shown under an icon

field: icon-view.default-zoom-level
	type: string
	allowed: smallest, smaller, small, standard, large, larger, largest
	default: standard

field: icon-view.labels-beside-icons
	type: bool
	default: false

field: icon-view.text-ellipsis-limit
	type: string-array
	default: 3
	desc: Lines of a name under an icon before it is cut short

field: icon-view.thumbnail-size
	type: int
	default: 64
	desc: Thumbnail size in pixels

field: list-view.column-max-widths
	type: string-array
	desc: Widest each column may get, in pixels. Set by dragging.

field: list-view.default-column-order
	type: string-array
	default: name, where, size, extension, type, date_modified, owner, group, permissions

field: list-view.default-visible-columns
	type: string-array
	default: name, size, extension, type, date_modified, owner, group, permissions

field: list-view.default-zoom-level
	type: string
	allowed: smallest, smaller, small, standard, large, larger, largest
	default: small

field: list-view.enable-folder-expansion
	type: bool
	default: true
	desc: Let a folder be expanded in place to show what is in it

field: plugins.disabled-actions
	type: string-array

field: plugins.disabled-extensions
	type: string-array

field: plugins.disabled-scripts
	type: string-array

field: preferences.always-show-tabs
	type: bool
	default: false
	desc: Show the tab strip even with only one tab

field: preferences.always-use-browser
	type: bool
	default: true
	desc: Every window is a browser window, with a toolbar and history

field: preferences.bulk-rename-tool
	type: string
	default: ""
	desc: Program run for a bulk rename

field: preferences.click-double-parent-folder
	type: bool
	default: false
	desc: Double-clicking empty space goes to the parent folder

field: preferences.click-policy
	type: string
	allowed: single, double
	default: double
	desc: One click or two to open a file

field: preferences.close-device-view-on-device-eject
	type: bool
	default: false
	desc: Close the window when its device is ejected, rather than going home

field: preferences.confirm-drag-copy
	type: bool
	default: false
	desc: Ask before a drop copies files

field: preferences.confirm-drag-move
	type: bool
	default: true
	desc: Ask before a drop moves files

field: preferences.confirm-move-to-trash
	type: bool
	default: true

field: preferences.confirm-trash
	type: bool
	default: true
	desc: Ask before deleting outright or emptying the Trash

field: preferences.confirm-many-items
	type: int
	default: 20
	desc: Ask before trashing or deleting this many items at once, whatever the two above say (0 for never)

field: preferences.date-format
	type: string
	allowed: locale, iso, informal
	default: iso

field: preferences.default-folder-viewer
	type: string
	allowed: icon-view, compact-view, list-view
	default: list-view

field: preferences.default-sort-in-reverse-order
	type: bool
	default: false

field: preferences.default-sort-order
	type: string
	allowed: manually, name, size, type, detailed_type, mtime, atime, trash-time
	default: name

field: preferences.deferred-attribute-preload-limit
	type: int
	default: 150
	desc: How many files a folder reads extra details for up front

field: preferences.desktop-is-home-dir
	type: bool
	default: false

field: preferences.detect-content
	type: bool
	default: false
	desc: Look inside mounted media to suggest a program for it

field: preferences.disable-menu-warning
	type: bool
	default: false
	desc: Skip the explanation shown when the menu bar is turned off

field: preferences.enable-delete
	type: bool
	default: true
	desc: Offer Delete, which skips the Trash

field: preferences.enable-mime-actions-make-executable
	type: bool
	default: false
	desc: Offer to make an unknown file executable and run it

field: preferences.executable-text-activation
	type: string
	allowed: launch, display, ask
	default: ask
	desc: What opening an executable text file does

field: preferences.expand-row-on-dnd-dwell
	type: bool
	default: true
	desc: Hovering a folder during a drag opens it

field: preferences.ignore-view-metadata
	type: bool
	default: false
	desc: Use the default view and zoom everywhere, ignoring what each folder remembers

field: preferences.image-viewers-with-external-sort
	type: string-array
	default: xviewer, feh, sxiv
	desc: Image viewers that are told the current sort order

field: preferences.inherit-folder-viewer
	type: bool
	default: false
	desc: A folder opens in the view its parent used

field: preferences.inherit-show-thumbnails
	type: bool
	default: true
	desc: A folder shows thumbnails if its parent did

field: preferences.last-server-connect-method
	type: int
	default: 2

field: preferences.mouse-back-button
	type: int
	default: 8
	desc: Mouse button that goes back

field: preferences.mouse-forward-button
	type: int
	default: 9
	desc: Mouse button that goes forward

field: preferences.mouse-use-extra-buttons
	type: bool
	default: true
	desc: Use the extra mouse buttons for back and forward

field: preferences.never-queue-file-ops
	type: bool
	default: false
	desc: Start every file operation at once rather than queueing them

field: preferences.rename-selects-whole-name
	type: bool
	default: true
	desc: Renaming selects the whole name, extension included

field: preferences.quick-renames-with-pause-in-between
	type: bool
	default: false
	desc: Two slow clicks on a name start a rename

field: preferences.show-advanced-permissions
	type: bool
	default: false

field: preferences.show-bookmarks-in-to-menus
	type: bool
	default: true
	desc: List bookmarks in the Move To and Copy To menus

field: preferences.show-compact-view-icon-toolbar
	type: bool
	default: true

field: preferences.show-computer-icon-toolbar
	type: bool
	default: false

field: preferences.show-directory-item-counts
	type: string
	allowed: always, local-only, never
	default: local-only
	desc: When to count what is in a folder

field: preferences.show-edit-icon-toolbar
	type: bool
	default: true
	desc: Show the button that swaps the path buttons for a typed location

field: preferences.show-full-path-titles
	type: bool
	default: false
	desc: Show the whole path in the title bar and on tabs

field: preferences.show-hidden-files
	type: bool
	default: false

field: preferences.show-home-icon-toolbar
	type: bool
	default: false

field: preferences.show-icon-view-icon-toolbar
	type: bool
	default: true

field: preferences.show-image-thumbnails
	type: string
	allowed: always, local-only, never
	default: local-only
	desc: When to draw thumbnails

field: preferences.show-list-view-icon-toolbar
	type: bool
	default: true

field: preferences.show-location-entry
	type: bool
	default: false
	desc: Start with a typed location instead of path buttons

field: preferences.show-new-folder-icon-toolbar
	type: bool
	default: false

field: preferences.show-next-icon-toolbar
	type: bool
	default: true

field: preferences.show-open-in-terminal-toolbar
	type: bool
	default: false

field: preferences.show-places-in-to-menus
	type: bool
	default: true
	desc: List places in the Move To and Copy To menus

field: preferences.show-previous-icon-toolbar
	type: bool
	default: true

field: preferences.show-reload-icon-toolbar
	type: bool
	default: false

field: preferences.show-root-warning
	type: bool
	default: true
	desc: Warn before opening a window as root

field: preferences.show-search-icon-toolbar
	type: bool
	default: true

field: preferences.show-shortcut-extension
	type: bool
	default: false
	desc: Count a shortcut's .lnk or .desktop extension as part of its name

field: preferences.show-show-thumbnails-toolbar
	type: bool
	default: false

field: preferences.show-toggle-extra-pane-toolbar
	type: bool
	default: false

field: preferences.show-up-icon-toolbar
	type: bool
	default: true

field: preferences.size-prefixes
	type: string
	allowed: base-10, base-10-full, base-2, base-2-full
	default: base-2
	desc: Whether sizes count in 1024s or 1000s

field: preferences.sort-directories-first
	type: bool
	default: true

field: preferences.sort-favorites-first
	type: bool
	default: true

field: preferences.start-with-dual-pane
	type: bool
	default: false

field: preferences.swap-trash-delete
	type: bool
	default: false
	desc: Swap the Trash and Delete keys

field: preferences.tab-width-max-percent
	type: int
	default: 25
	desc: Widest a tab may get, as a percentage of the tab strip

field: preferences.tab-width-min-percent
	type: int
	default: 10
	desc: Narrowest a tab may get, as a percentage of the tab strip

field: preferences.tabs-open-position
	type: string
	allowed: after-current-tab, end
	default: after-current-tab
	desc: Where a new tab goes

field: preferences.thumbnail-limit
	type: int
	default: 1048576
	desc: Largest image a thumbnail is made for, in bytes

field: preferences.thumbnail-cache-max-mb
	type: int
	default: 512
	desc: Largest the thumbnail cache may get, in megabytes. 0 for no limit.

field: preferences.thumbnail-cache-max-days
	type: int
	default: 180
	desc: Discard a thumbnail unused for this many days. 0 to keep them however old.

field: preferences.thumbnail-threads
	type: int
	default: -1
	desc: Threads used to make thumbnails, -1 to decide automatically. Takes effect on restart.

field: preferences.tooltips-in-icon-view
	type: bool
	default: false

field: preferences.tooltips-in-list-view
	type: bool
	default: false

field: preferences.tooltips-show-access-date
	type: bool
	default: false

field: preferences.tooltips-show-birth-date
	type: bool
	default: false
	desc: Show the creation date in a tooltip

field: preferences.tooltips-show-file-type
	type: bool
	default: false

field: preferences.tooltips-show-mod-date
	type: bool
	default: false

field: preferences.tooltips-show-path
	type: bool
	default: false

field: preferences.treat-root-as-normal
	type: bool
	default: false
	desc: Drop the safeguards that apply when running as root

field: preferences.window-per-process
	type: bool
	default: true
	desc: Each new window runs as its own process

field: preferences.menu-config.background-menu-compress
	type: bool
	default: true

field: preferences.menu-config.background-menu-copy-path
	type: bool
	default: true

field: preferences.menu-config.background-menu-create-new-folder
	type: bool
	default: true

field: preferences.menu-config.background-menu-open-as-root
	type: bool
	default: true

field: preferences.menu-config.background-menu-open-in-terminal
	type: bool
	default: true

field: preferences.menu-config.background-menu-paste
	type: bool
	default: true

field: preferences.menu-config.background-menu-properties
	type: bool
	default: true

field: preferences.menu-config.background-menu-scripts
	type: bool
	default: true

field: preferences.menu-config.background-menu-show-hidden-files
	type: bool
	default: true

field: preferences.menu-config.iconview-menu-arrange-items
	type: bool
	default: true

field: preferences.menu-config.iconview-menu-organize-by-name
	type: bool
	default: true

field: preferences.menu-config.selection-menu-compress
	type: bool
	default: true

field: preferences.menu-config.selection-menu-copy
	type: bool
	default: true

field: preferences.menu-config.selection-menu-copy-to
	type: bool
	default: false

field: preferences.menu-config.selection-menu-copy-path
	type: bool
	default: true

field: preferences.menu-config.selection-menu-cut
	type: bool
	default: true

field: preferences.menu-config.selection-menu-duplicate
	type: bool
	default: false

field: preferences.menu-config.selection-menu-extract
	type: bool
	default: true

field: preferences.menu-config.selection-menu-favorite
	type: bool
	default: true

field: preferences.menu-config.selection-menu-make-link
	type: bool
	default: true

field: preferences.menu-config.selection-menu-move-to
	type: bool
	default: false

field: preferences.menu-config.selection-menu-move-to-trash
	type: bool
	default: true

field: preferences.menu-config.selection-menu-open
	type: bool
	default: true

field: preferences.menu-config.selection-menu-open-as-root
	type: bool
	default: true

field: preferences.menu-config.selection-menu-open-in-new-tab
	type: bool
	default: true

field: preferences.menu-config.selection-menu-open-in-new-window
	type: bool
	default: true

field: preferences.menu-config.selection-menu-open-in-terminal
	type: bool
	default: true

field: preferences.menu-config.selection-menu-paste
	type: bool
	default: true

field: preferences.menu-config.selection-menu-pin
	type: bool
	default: true

field: preferences.menu-config.selection-menu-properties
	type: bool
	default: true

field: preferences.menu-config.selection-menu-rename
	type: bool
	default: true

field: preferences.menu-config.selection-menu-scripts
	type: bool
	default: true

field: search.disabled-search-helpers
	type: string-array
	desc: Content-search helpers to skip

field: search.group-by-folder
	type: bool
	default: false
	desc: Group results under the folder holding them instead of one flat list

field: search.name-location-split
	type: int
	default: 0
	desc: Percent of the row the Name column takes in results. 0 fits both to their contents.

field: search.search-content-case-sensitive
	type: bool
	default: false

field: search.search-content-use-raw
	type: bool
	default: false
	desc: Match content as raw bytes rather than text

field: search.search-content-use-regex
	type: bool
	default: false

field: search.search-file-case-sensitive
	type: bool
	default: false

field: search.search-files-recursively
	type: bool
	default: true

field: search.search-files-use-regex
	type: bool
	default: false

field: search.search-regex-format
	type: string
	default: pcre
	desc: Regex flavour: pcre or javascript

field: search.search-reverse-sort
	type: bool
	default: false

field: search.search-skip-folders
	type: string-array
	default: /dev, /proc, /sys, dosdevices, .git
	desc: Paths or folder names a search never enters

field: search.search-sort-column
	type: string
	default: ""

field: search.search-visible-columns
	type: string-array

field: sidebar-panels.tree.show-only-directories
	type: bool
	default: true
	desc: Show only folders in the tree side pane

field: state.first-run-done
	type: bool
	default: false
	desc: Clear this to get the platform's default bookmarks back on the next start.

field: thumbnailers.disable
	type: string-array
	desc: Mime types not to use an external thumbnailer for

field: thumbnailers.disable-all
	type: bool
	default: false

field: windows.path-separator
	type: string
	allowed: backslash, slash
	default: backslash
	desc: Which separator paths are shown with

field: windows.allow-slash-input
	type: bool
	default: true
	desc: Accept a forward slash as a separator in a typed location

field: windows.show-dot-files
	type: bool
	default: false
	desc: Show files whose name starts with a dot. Separate from the Windows hidden flag.

field: windows.use-search-index
	type: bool
	default: false
	desc: Answer a search from the Windows Search index where the folder is indexed

field: windows.associations
	type: string-array
	desc: The program to open a type with, as <extension>=<command line> with %1 for the file

field: windows.terminal-candidates
	type: string-array
	default: wt.exe, pwsh.exe, powershell.exe, cmd.exe
	desc: Terminals to try for Open in Terminal, in order

field: window-state.bookmarks-expanded
	type: bool
	default: true

field: window-state.devices-expanded
	type: bool
	default: true

field: window-state.geometry
	type: string
	default: ""

field: window-state.maximized
	type: bool
	default: false

field: window-state.my-computer-expanded
	type: bool
	default: true

field: window-state.network-expanded
	type: bool
	default: true

field: window-state.side-pane-view
	type: string
	default: places

field: window-state.sidebar-bookmark-breakpoint
	type: int
	default: -1
	desc: Bookmark index where the dedicated sidebar section starts

field: window-state.sidebar-width
	type: int
	default: 240

field: window-state.start-with-location-bar
	type: bool
	default: true
	desc: Show the location bar in new windows

field: window-state.start-with-menu-bar
	type: bool
	default: true
	desc: Show the menu bar in new windows

field: window-state.start-with-sidebar
	type: bool
	default: true
	desc: Show the side pane in new windows

field: window-state.start-with-status-bar
	type: bool
	default: true
	desc: Show the status bar in new windows

field: window-state.start-with-toolbar
	type: bool
	default: true
	desc: Show the toolbar in new windows
